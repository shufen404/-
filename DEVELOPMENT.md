# 牛来桌宠开发流程

## 两条轨道

- `preview.cmd` 或 `npm run preview`：直接运行当前源码，使用独立的 `niulai-desktop-pet-preview` 用户数据，不修改正式版计薪状态。
- `release-final`：只放已经核对完成、准备交付的安装包和便携版。没有完成核对前不覆盖这里的文件。

## 功能边界

- `src/main.js`：窗口、托盘、快捷键、外部窗口和 IPC 编排。
- `src/renderer.js`：桌宠状态、计薪、语音、点击和回车行为。
- `src/panel.js`：设置、工具和播放器面板视图。
- `src/preload.js`：唯一的渲染进程 API 边界；新增能力先加这里，再接主进程或页面。
- `src/panel.css` / `src/pet.css`：只负责外观，不在 CSS 中改变业务状态。

新增功能按“状态、IPC、视图、验证”四步加入。不要在多个页面各自复制一套状态切换逻辑；工作模式和个人模式必须通过统一的状态函数改变。每次预览只验证源码，确认功能后再统一构建安装包。

## 预览检查清单

1. 双击 `preview.cmd`。
2. 检查桌宠点击、个人/工作模式、Enter、设置和工具入口。
3. 检查新增功能与现有面板是否互相遮挡或抢夺快捷键。
4. 通过后运行 `npm run check`，再生成最终安装包。
